import { ActivityCalendar } from "activity-calendar-react"

export default function Calendar({ data }) {
  return (
    <div>
      {/* <ActivityCalendar key={data} sampleData={data} showMonth={true} showDay={true} /> */}
    </div>
  )
}
